class program{
    static void Main (string[] args){
    
         Console.WriteLine("day of week");
            string day = Console.ReadLine();
               if (int.TryParse("day of week", out int number) && number >= 0 && number <=7) //ยอมแพ้แล้วค่าาาาาาาา
            {
        Console.WriteLine("time of day");
                string time = Console.ReadLine();
                if (int.TryParse("time of day", out int number) && number >= 8 && number <=18)
            }
        Console.WriteLine("menu");
                string menu = Console.ReadLine();
    }

            if (menu == "Breakfast set")
            
            {
             if (menu == "Weekend set") 
            
            }
                Console.ReadLine()
    }
  







    


    

